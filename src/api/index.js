import { users, fetchAll, getById } from './fake.api/user.api'
import professions from './fake.api/professions.api'

const API = {
    users,
    fetchAll,
    getById,
    professions
}

export default API
